start: Hello, how can I help you today?

news: |
<speak>
    Sure, here's what I've found in the news
    for {{ topic }}. Apparently,
    {% for result in opinions %}
        {{ result.source }} says that {{ result.article }}
        ,
        {% if result.and_say %}
            and
        {% endif %}
    {% endfor %}
    according to our sources.
</speak>